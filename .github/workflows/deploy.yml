name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node environment
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        # Remove this step if it's a static HTML project without a package.json
        run: npm install

      - name: Build project
        # Remove this step if it's a static HTML project without a build step
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # ------------------------------------------------------------------------
          # FOLDER TO DEPLOY:
          # - If Vite / React / Vue (default): './dist'
          # - If Next.js (static export):      './out'
          # - If static HTML (no build step):  '.'
          # ------------------------------------------------------------------------
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
